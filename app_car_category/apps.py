from django.apps import AppConfig

class AppCarCategoryConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app_car_category'
    verbose_name = 'Categorías de automóviles'